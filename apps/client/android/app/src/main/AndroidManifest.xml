<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Health Connect asks an app to explain itself: tapping the privacy-policy link in
             the permission sheet opens this. Both forms are needed because Android changed the
             mechanism at 14 — an activity for 13 and below, an alias for 14 and later — and a
             phone that finds neither shows the person a dead link at the exact moment they are
             deciding whether to trust us with their heart rate. -->
        <activity
            android:name="com.fit_up.health.capacitor.PermissionsRationaleActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
            </intent-filter>
        </activity>
        <activity-alias
            android:name="ViewPermissionUsageActivity"
            android:exported="true"
            android:targetActivity="com.fit_up.health.capacitor.PermissionsRationaleActivity"
            android:permission="android.permission.START_VIEW_PERMISSION_USAGE">
            <intent-filter>
                <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />
                <category android:name="android.intent.category.HEALTH_PERMISSIONS" />
            </intent-filter>
        </activity-alias>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Workout-day reminder: lets the local-notifications plugin fire at the exact minute
         on Android 12+. Where it isn't granted, scheduling silently falls back to inexact
         alarms — the reminder still arrives, just not to the minute. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- A heart-rate strap, over Bluetooth LE. Nothing here reaches a network: the app talks
         to a device in the room and the readings go into the session on this phone, which is
         the same promise MOBILE.md makes about everything else in this build.

         `neverForLocation` is the load-bearing attribute. Android ties BLE scanning to
         location by default, on the reasoning that a scan can be used to work out where
         somebody is — this flag is the app declaring it will not, and it is what stops the OS
         asking for location permission on Android 12 and later.

         On Android 11 and below there is no such flag and the OS requires location for any
         scan at all, so the two location permissions below exist for those devices only and
         are capped so they can never apply on a phone new enough to use the flag. That cap is
         the whole reason they are written out rather than left to the plugin's defaults.

         Cafe Bazaar asks what each of these is for — see docs/store/listing.fa.md. -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" android:maxSdkVersion="30" />

    <!-- Health Connect: the sessions somebody's watch already wrote, read locally.
         `queries` is what lets this app see that the Health Connect package exists at all —
         Android 11 hides other packages by default, and without this the availability check
         answers "not installed" on a phone where it plainly is.

         Two permissions and no more. Sessions, and the heart rate inside them. Not steps, not
         calories, and deliberately not READ_EXERCISE_ROUTE, which is a map of where somebody
         runs and which this app has nowhere to put. Nothing read here leaves the device — see
         docs/MOBILE.md, and the justification in docs/store/listing.fa.md. -->
    <queries>
        <package android:name="com.google.android.apps.healthdata" />
    </queries>
    <uses-permission android:name="android.permission.health.READ_EXERCISE" />
    <uses-permission android:name="android.permission.health.READ_HEART_RATE" />
</manifest>
